{
  "name": "kittingflow",
  "version": "1.0.0",
  "description": "KittingFlow 製造補助システム (Apps Script + VSCode)",
  "scripts": {
    "deploy": "clasp push; clasp deploy -d 'Auto deploy from VSCode'",
    "push": "clasp push",
    "pull": "clasp pull",
    "gas:deploy:init": "node scripts/deploy-gas.js --init",
    "gas:deploy": "node scripts/deploy-gas.js",
    "gas:deploy:set-netlify": "node scripts/deploy-gas.js --set-netlify"
  },
  "dependencies": {},
  "devDependencies": {
    "@google/clasp": "^2.4.0"
  }
}
