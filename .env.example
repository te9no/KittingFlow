GAS_ENDPOINT=https://script.google.com/macros/s/XXXXX/exec
# Optional: shared token — must match Apps Script Script Properties API_TOKEN
API_TOKEN=
# Optional: restrict CORS origin (defaults to *)
ALLOW_ORIGIN=*
